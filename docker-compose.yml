# Standalone deployment for the Bangladesh Betar public portal (Next.js).
#
#   docker compose up -d --build      # build + run
#   docker compose up -d              # apply env changes (recreates, no rebuild)
#   docker compose logs -f            # watch
#   docker compose down               # stop
#
# The browser-facing URLs are read from the environment below (defaults live in
# .env — edit it and re-run `up -d` to point at a different API host; the image
# does NOT need rebuilding).
services:
  public:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_ADSENSE_CLIENT: ${NEXT_PUBLIC_ADSENSE_CLIENT:-}
        NEXT_PUBLIC_ADSENSE_NEWS_LEADERBOARD_SLOT: ${NEXT_PUBLIC_ADSENSE_NEWS_LEADERBOARD_SLOT:-}
        NEXT_PUBLIC_ADSENSE_NEWS_RECTANGLE_SLOT: ${NEXT_PUBLIC_ADSENSE_NEWS_RECTANGLE_SLOT:-}
        NEXT_PUBLIC_ADSENSE_NEWS_IN_ARTICLE_SLOT: ${NEXT_PUBLIC_ADSENSE_NEWS_IN_ARTICLE_SLOT:-}
    image: betar-public:latest
    container_name: betar_public
    restart: unless-stopped
    ports:
      - "${PUBLIC_PORT:-15001}:15001"
    environment:
      # Blank => the app derives these from the browser's host (works on any
      # LAN IP with no rebuild). Set to pin a fixed host (production domain).
      NEXT_PUBLIC_API_BASE: ${NEXT_PUBLIC_API_BASE:-}
      NEXT_PUBLIC_LIVEKIT_URL: ${NEXT_PUBLIC_LIVEKIT_URL:-}
      # Server-only addresses used to generate social preview metadata.
      API_INTERNAL_BASE: ${API_INTERNAL_BASE:-http://host.docker.internal:15000/api/v1}
      PUBLIC_SITE_URL: ${PUBLIC_SITE_URL:-http://localhost:15001}
