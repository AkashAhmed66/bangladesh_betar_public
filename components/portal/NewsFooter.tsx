import PortalFooter from "@/components/portal/PortalFooter";

/** Keep News visually and functionally aligned with the shared Watch/Listen footer. */
export default function NewsFooter() {
  return <PortalFooter portal="news" />;
}
